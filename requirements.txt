# ─────────────────────────────────────────────────────────────────────────────
# EmailTriage Sovereign Agent v5.0.0 — Requirements
# HuggingFace Spaces deployment (CPU-safe, no GPU required for demo)
# Training dependencies are optional (install with: pip install -r requirements-train.txt)
# ─────────────────────────────────────────────────────────────────────────────

# ── Core UI ───────────────────────────────────────────────────────────────────
gradio>=4.40.0,<5.0.0
huggingface_hub<0.25.0

# ── Environment (pure-Python, no GPU needed) ──────────────────────────────────
# All dependencies are in stdlib (json, random, re, dataclasses, collections)
# No extra packages required for environment.py

# ── Optional: Training stack (GPU instance required) ─────────────────────────
# Uncomment when running train_frontier_v5.py on A100/H100:
# torch>=2.3.0
# transformers>=4.44.0
# datasets>=2.20.0
# trl>=0.10.0
# unsloth[colab-new]>=2024.9
# bitsandbytes>=0.43.0
# accelerate>=0.33.0
# peft>=0.12.0
# xformers>=0.0.27